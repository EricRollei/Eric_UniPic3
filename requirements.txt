# Eric UniPic3 Requirements
# These are the additional dependencies beyond ComfyUI's base requirements

# Diffusers with QwenImageEditPlusPipeline support
diffusers>=0.35.0

# Transformers for Qwen2.5-VL text encoder
transformers>=4.45.0

# Image processing
Pillow>=9.0.0

# PyTorch (should already be installed with ComfyUI)
# torch>=2.0.0
